+<h2>Create a New Recipient</h2>

 <%= form_for [:admin, :charity, @recipient] do |f| %>
 <%= f.label :name %>
 <%= f.text_field :name %>
 <%= f.label :description %>
 <%= f.text_field :description %>
 <%= f.submit "Create Recipient" %>
 <% end %>
